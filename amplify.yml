version: 1

backend:
  phases:
    build:
      commands:
        - echo "No backend amplify resources"

frontend:
  phases:
    preBuild:
      commands:
        - npm ci

    build:
      commands:
        - echo "Build complete"

  artifacts:
    baseDirectory: /
    files:
      - '**/*'

  cache:
    paths:
      - node_modules/**/*